/* 
    Basic arithmetic operations
        +   takes top two values from stack and puts sum on the stack
        -   takes top two values from the stack and puts difference between *second* and *first* on the stack
*/
32 5 + .
10 5 - .
